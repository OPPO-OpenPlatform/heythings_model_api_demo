/* Generated by the heythings model compiler.  DO NOT EDIT! */
/* Generated from: meshNodeFeature.proto */

#include <errno.h>
#include <assert.h>
#include "meshNodeFeature.pb-c.h"
#include "mesh_node_feature_service.h"
#include "hey/schema.h"

#define MESH_NODE_FEATURE_SERVICE (88 * 256)
#define PROP_IID_CNT 5

static const struct hey_schema_property mesh_node_feature_properties_iids[] = {
    [0] = { .iid = 1, .type = HEY_PROPERTY_TYPE_GENERIC },
    [1] = { .iid = 2, .type = HEY_PROPERTY_TYPE_ARRAY },
    [2] = { .iid = 3, .type = HEY_PROPERTY_TYPE_ARRAY },
    [3] = { .iid = 4, .type = HEY_PROPERTY_TYPE_ARRAY },
    [4] = { .iid = 5, .type = HEY_PROPERTY_TYPE_GENERIC },
};
static struct hey_service *mesh_node_feature_service;

static Iot__Service__MeshNodeFeature__MeshNetIndex *_mesh_node_feature_mesh_net_index = NULL;
struct mesh_node_feature_mesh_net_index *mesh_node_feature_mesh_net_index_unpack(const uint8_t *data, size_t len)
{
    _mesh_node_feature_mesh_net_index = iot__service__mesh_node_feature__mesh_net_index__unpack(NULL, len, data);
    if (NULL == _mesh_node_feature_mesh_net_index) {
        return NULL;
    }
    struct mesh_node_feature_mesh_net_index *hey_struct = malloc(sizeof(struct mesh_node_feature_mesh_net_index));
    if (NULL == hey_struct) {
        goto out1;
    }
    hey_struct->netidx = _mesh_node_feature_mesh_net_index->netidx;
    return hey_struct;
out1:
    iot__service__mesh_node_feature__mesh_net_index__free_unpacked(_mesh_node_feature_mesh_net_index, NULL);
    return NULL;
}

void mesh_node_feature_mesh_net_index_free_unpacked(struct mesh_node_feature_mesh_net_index *data)
{
    assert(NULL != data);
    iot__service__mesh_node_feature__mesh_net_index__free_unpacked(_mesh_node_feature_mesh_net_index, NULL);
    free(data);
}

int32_t mesh_node_feature_mesh_net_index_get_packed_size(const struct mesh_node_feature_mesh_net_index *data)
{
    assert(data);
    int ret = 0;
    Iot__Service__MeshNodeFeature__MeshNetIndex *pb_struct = malloc(sizeof(Iot__Service__MeshNodeFeature__MeshNetIndex));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__mesh_node_feature__mesh_net_index__init(pb_struct);
    pb_struct->netidx = data->netidx;
    ret = iot__service__mesh_node_feature__mesh_net_index__get_packed_size(pb_struct);
    free(pb_struct);
    return ret;
}

int32_t mesh_node_feature_mesh_net_index_pack(const struct mesh_node_feature_mesh_net_index *data, uint8_t *out)
{
    assert(data);
    int ret = 0;
    Iot__Service__MeshNodeFeature__MeshNetIndex *pb_struct = malloc(sizeof(Iot__Service__MeshNodeFeature__MeshNetIndex));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__mesh_node_feature__mesh_net_index__init(pb_struct);
    pb_struct->netidx = data->netidx;
    ret = iot__service__mesh_node_feature__mesh_net_index__pack(pb_struct, out);
    free(pb_struct);
    return ret;
}

static Iot__Service__MeshNodeFeature__MeshAppIndex *_mesh_node_feature_mesh_app_index = NULL;
struct mesh_node_feature_mesh_app_index *mesh_node_feature_mesh_app_index_unpack(const uint8_t *data, size_t len)
{
    _mesh_node_feature_mesh_app_index = iot__service__mesh_node_feature__mesh_app_index__unpack(NULL, len, data);
    if (NULL == _mesh_node_feature_mesh_app_index) {
        return NULL;
    }
    struct mesh_node_feature_mesh_app_index *hey_struct = malloc(sizeof(struct mesh_node_feature_mesh_app_index));
    if (NULL == hey_struct) {
        goto out1;
    }
    hey_struct->appidx = _mesh_node_feature_mesh_app_index->appidx;
    return hey_struct;
out1:
    iot__service__mesh_node_feature__mesh_app_index__free_unpacked(_mesh_node_feature_mesh_app_index, NULL);
    return NULL;
}

void mesh_node_feature_mesh_app_index_free_unpacked(struct mesh_node_feature_mesh_app_index *data)
{
    assert(NULL != data);
    iot__service__mesh_node_feature__mesh_app_index__free_unpacked(_mesh_node_feature_mesh_app_index, NULL);
    free(data);
}

int32_t mesh_node_feature_mesh_app_index_get_packed_size(const struct mesh_node_feature_mesh_app_index *data)
{
    assert(data);
    int ret = 0;
    Iot__Service__MeshNodeFeature__MeshAppIndex *pb_struct = malloc(sizeof(Iot__Service__MeshNodeFeature__MeshAppIndex));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__mesh_node_feature__mesh_app_index__init(pb_struct);
    pb_struct->appidx = data->appidx;
    ret = iot__service__mesh_node_feature__mesh_app_index__get_packed_size(pb_struct);
    free(pb_struct);
    return ret;
}

int32_t mesh_node_feature_mesh_app_index_pack(const struct mesh_node_feature_mesh_app_index *data, uint8_t *out)
{
    assert(data);
    int ret = 0;
    Iot__Service__MeshNodeFeature__MeshAppIndex *pb_struct = malloc(sizeof(Iot__Service__MeshNodeFeature__MeshAppIndex));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__mesh_node_feature__mesh_app_index__init(pb_struct);
    pb_struct->appidx = data->appidx;
    ret = iot__service__mesh_node_feature__mesh_app_index__pack(pb_struct, out);
    free(pb_struct);
    return ret;
}

static Iot__Service__MeshNodeFeature__MeshElement *_mesh_node_feature_mesh_element = NULL;
struct mesh_node_feature_mesh_element *mesh_node_feature_mesh_element_unpack(const uint8_t *data, size_t len)
{
    _mesh_node_feature_mesh_element = iot__service__mesh_node_feature__mesh_element__unpack(NULL, len, data);
    if (NULL == _mesh_node_feature_mesh_element) {
        return NULL;
    }
    struct mesh_node_feature_mesh_element *hey_struct = malloc(sizeof(struct mesh_node_feature_mesh_element));
    if (NULL == hey_struct) {
        goto out1;
    }
    struct mesh_element_mesh_model **meshmodel = NULL;
    meshmodel = calloc(_mesh_node_feature_mesh_element->n_meshmodel, sizeof(struct mesh_element_mesh_model *));
    if (NULL == meshmodel) {
        goto out2;
    }
    for (int i = 0; i < _mesh_node_feature_mesh_element->n_meshmodel; i++) {
        meshmodel[i] = malloc(sizeof(struct mesh_element_mesh_model));
        if (NULL == meshmodel[i]) {
            goto out3;
        }
        meshmodel[i]->companyid = _mesh_node_feature_mesh_element->meshmodel[i]->companyid;
        meshmodel[i]->modelid = _mesh_node_feature_mesh_element->meshmodel[i]->modelid;
        meshmodel[i]->pub = malloc(sizeof(*meshmodel[i]->pub));
        if (NULL == meshmodel[i]->pub) {
            goto out3;
        }
        meshmodel[i]->pub->dst = _mesh_node_feature_mesh_element->meshmodel[i]->pub->dst;
        meshmodel[i]->pub->key = _mesh_node_feature_mesh_element->meshmodel[i]->pub->key;
        meshmodel[i]->n_appkeyidx = _mesh_node_feature_mesh_element->meshmodel[i]->n_appkeyidx;
        memcpy(meshmodel[i]->appkeyidx, _mesh_node_feature_mesh_element->meshmodel[i]->appkeyidx, sizeof(uint32_t) * meshmodel[i]->n_appkeyidx);
        meshmodel[i]->n_groups = _mesh_node_feature_mesh_element->meshmodel[i]->n_groups;
        memcpy(meshmodel[i]->groups, _mesh_node_feature_mesh_element->meshmodel[i]->groups, sizeof(uint32_t) * meshmodel[i]->n_groups);
    }
    hey_struct->addr = _mesh_node_feature_mesh_element->addr;
    hey_struct->n_meshmodel = _mesh_node_feature_mesh_element->n_meshmodel;
    hey_struct->meshmodel = meshmodel;
    return hey_struct;
out3:
    for (int i = 0; i < _mesh_node_feature_mesh_element->n_meshmodel; i++) {
        if (NULL == meshmodel[i])
            break;
        free(meshmodel[i]->pub);
        free(meshmodel[i]);
    }
    free(meshmodel);
out2:
    free(hey_struct);
out1:
    iot__service__mesh_node_feature__mesh_element__free_unpacked(_mesh_node_feature_mesh_element, NULL);
    return NULL;
}

void mesh_node_feature_mesh_element_free_unpacked(struct mesh_node_feature_mesh_element *data)
{
    assert(NULL != data);
    iot__service__mesh_node_feature__mesh_element__free_unpacked(_mesh_node_feature_mesh_element, NULL);
    for (int i = 0; i < data->n_meshmodel; i++) {
        free(data->meshmodel[i]->pub);
        free(data->meshmodel[i]);
    }
    free(data->meshmodel);
    free(data);
}

int32_t mesh_node_feature_mesh_element_get_packed_size(const struct mesh_node_feature_mesh_element *data)
{
    assert(data);
    int ret = 0;
    Iot__Service__MeshNodeFeature__MeshElement *pb_struct = malloc(sizeof(Iot__Service__MeshNodeFeature__MeshElement));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__mesh_node_feature__mesh_element__init(pb_struct);
    Iot__Service__MeshNodeFeature__MeshElement__MeshModel **meshmodel = NULL;
    if (data->n_meshmodel) {
        meshmodel = calloc(data->n_meshmodel, sizeof(Iot__Service__MeshNodeFeature__MeshElement__MeshModel *));
        if (NULL == meshmodel) {
            ret = -1;
            goto out;
        }
        for (int i = 0; i < data->n_meshmodel; i++) {
            meshmodel[i] = malloc(sizeof(Iot__Service__MeshNodeFeature__MeshElement__MeshModel));
            if (NULL == meshmodel[i]) {
                ret = -1;
                goto out;
            }
            iot__service__mesh_node_feature__mesh_element__mesh_model__init(meshmodel[i]);
            meshmodel[i]->companyid = data->meshmodel[i]->companyid;
            meshmodel[i]->modelid = data->meshmodel[i]->modelid;
            meshmodel[i]->pub = data->meshmodel[i]->pub;
            meshmodel[i]->n_appkeyidx = data->meshmodel[i]->n_appkeyidx;
            meshmodel[i]->appkeyidx = data->meshmodel[i]->appkeyidx;
            meshmodel[i]->n_groups = data->meshmodel[i]->n_groups;
            meshmodel[i]->groups = data->meshmodel[i]->groups;
        }
    }
    pb_struct->addr = data->addr;
    pb_struct->n_meshmodel = data->n_meshmodel;
    pb_struct->meshmodel = meshmodel;
    ret = iot__service__mesh_node_feature__mesh_element__get_packed_size(pb_struct);
out:
    for (int i = 0; i < data->n_meshmodel; i++) {
        if (meshmodel[i] == NULL)
            break;
        free(meshmodel[i]);
    }
    free(meshmodel);
    free(pb_struct);
    return ret;
}

int32_t mesh_node_feature_mesh_element_pack(const struct mesh_node_feature_mesh_element *data, uint8_t *out)
{
    assert(data);
    int ret = 0;
    Iot__Service__MeshNodeFeature__MeshElement *pb_struct = malloc(sizeof(Iot__Service__MeshNodeFeature__MeshElement));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__mesh_node_feature__mesh_element__init(pb_struct);
    Iot__Service__MeshNodeFeature__MeshElement__MeshModel **meshmodel = NULL;
    if (data->n_meshmodel) {
        meshmodel = calloc(data->n_meshmodel, sizeof(Iot__Service__MeshNodeFeature__MeshElement__MeshModel *));
        if (NULL == meshmodel) {
            ret = -1;
            goto out;
        }
        for (int i = 0; i < data->n_meshmodel; i++) {
            meshmodel[i] = malloc(sizeof(Iot__Service__MeshNodeFeature__MeshElement__MeshModel));
            if (NULL == meshmodel[i]) {
                ret = -1;
                goto out;
            }
            iot__service__mesh_node_feature__mesh_element__mesh_model__init(meshmodel[i]);
            meshmodel[i]->companyid = data->meshmodel[i]->companyid;
            meshmodel[i]->modelid = data->meshmodel[i]->modelid;
            meshmodel[i]->pub = data->meshmodel[i]->pub;
            meshmodel[i]->n_appkeyidx = data->meshmodel[i]->n_appkeyidx;
            meshmodel[i]->appkeyidx = data->meshmodel[i]->appkeyidx;
            meshmodel[i]->n_groups = data->meshmodel[i]->n_groups;
            meshmodel[i]->groups = data->meshmodel[i]->groups;
        }
    }
    pb_struct->addr = data->addr;
    pb_struct->n_meshmodel = data->n_meshmodel;
    pb_struct->meshmodel = meshmodel;
    ret = iot__service__mesh_node_feature__mesh_element__pack(pb_struct, out);
out:
    for (int i = 0; i < data->n_meshmodel; i++) {
        if (meshmodel[i] == NULL)
            break;
        free(meshmodel[i]);
    }
    free(meshmodel);
    free(pb_struct);
    return ret;
}

static int32_t service_mesh_node_feature_properties_write_cb(void *cb_data, size_t n_iid, uint32_t *iids,
                                                             size_t len, uint8_t *value)
{
    Iot__Service__MeshNodeFeature__Properties *props = iot__service__mesh_node_feature__properties__unpack(NULL, len, value);
    if (NULL == props) {
        return EINVAL;
    }

    int32_t ret = 0;
    struct mesh_node_feature_properties properties = { 0 };
    struct hey_array_node **meshelement = NULL;
    struct hey_array_node **meshnetindex = NULL;
    struct hey_array_node **meshappindex = NULL;
    properties.primaryelemaddr = props->primaryelemaddr;
    if (props->n_meshelement) {
        meshelement = calloc(props->n_meshelement, sizeof(struct hey_array_node *));
        if (meshelement == NULL) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < props->n_meshelement; i++) {
            meshelement[i] = malloc(sizeof(struct hey_array_node));
            if (meshelement[i] == NULL) {
                ret = ENOMEM;
                goto out;
            }
            meshelement[i]->id = props->meshelement[i]->id;
            meshelement[i]->data.data = props->meshelement[i]->data.data;
            meshelement[i]->data.len = props->meshelement[i]->data.len;
        }
        properties.n_meshelement = props->n_meshelement;
        properties.meshelement = meshelement;
    }
    if (props->n_meshnetindex) {
        meshnetindex = calloc(props->n_meshnetindex, sizeof(struct hey_array_node *));
        if (meshnetindex == NULL) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < props->n_meshnetindex; i++) {
            meshnetindex[i] = malloc(sizeof(struct hey_array_node));
            if (meshnetindex[i] == NULL) {
                ret = ENOMEM;
                goto out;
            }
            meshnetindex[i]->id = props->meshnetindex[i]->id;
            meshnetindex[i]->data.data = props->meshnetindex[i]->data.data;
            meshnetindex[i]->data.len = props->meshnetindex[i]->data.len;
        }
        properties.n_meshnetindex = props->n_meshnetindex;
        properties.meshnetindex = meshnetindex;
    }
    if (props->n_meshappindex) {
        meshappindex = calloc(props->n_meshappindex, sizeof(struct hey_array_node *));
        if (meshappindex == NULL) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < props->n_meshappindex; i++) {
            meshappindex[i] = malloc(sizeof(struct hey_array_node));
            if (meshappindex[i] == NULL) {
                ret = ENOMEM;
                goto out;
            }
            meshappindex[i]->id = props->meshappindex[i]->id;
            meshappindex[i]->data.data = props->meshappindex[i]->data.data;
            meshappindex[i]->data.len = props->meshappindex[i]->data.len;
        }
        properties.n_meshappindex = props->n_meshappindex;
        properties.meshappindex = meshappindex;
    }
    properties.feature = props->feature;
    ret = mesh_node_feature_properties_write_cb(n_iid, iids, &properties);

out:
    if (meshelement) {
        for (int i = 0; i < props->n_meshelement; i++) {
            free(meshelement[i]);
        }
        free(meshelement);
    }
    if (meshnetindex) {
        for (int i = 0; i < props->n_meshnetindex; i++) {
            free(meshnetindex[i]);
        }
        free(meshnetindex);
    }
    if (meshappindex) {
        for (int i = 0; i < props->n_meshappindex; i++) {
            free(meshappindex[i]);
        }
        free(meshappindex);
    }
    iot__service__mesh_node_feature__properties__free_unpacked(props, NULL);
    return ret;
}

static int32_t service_mesh_node_feature_properties_array_add_cb(void *cb_data, uint32_t iid, size_t len, uint8_t *value)
{
    switch(iid) {
    case MESH_NODE_FEATURE_PROPERTY_MESHELEMENT:
        return mesh_node_feature_meshelement_array_add_cb(len, value);
    case MESH_NODE_FEATURE_PROPERTY_MESHNETINDEX:
        return mesh_node_feature_meshnetindex_array_add_cb(len, value);
    case MESH_NODE_FEATURE_PROPERTY_MESHAPPINDEX:
        return mesh_node_feature_meshappindex_array_add_cb(len, value);
    default:
        return -1;
    }
}

static int32_t service_mesh_node_feature_properties_array_del_cb(void *cb_data, uint32_t iid, size_t n_id, uint32_t *ids)
{
    switch(iid) {
    case MESH_NODE_FEATURE_PROPERTY_MESHELEMENT:
        return mesh_node_feature_meshelement_array_del_cb(n_id, ids);
    case MESH_NODE_FEATURE_PROPERTY_MESHNETINDEX:
        return mesh_node_feature_meshnetindex_array_del_cb(n_id, ids);
    case MESH_NODE_FEATURE_PROPERTY_MESHAPPINDEX:
        return mesh_node_feature_meshappindex_array_del_cb(n_id, ids);
    default:
        return -1;
    }
}

static int32_t service_mesh_node_feature_properties_array_replace_cb(void *cb_data, uint32_t iid, uint32_t id, size_t len, uint8_t *value)
{
    switch(iid) {
    case MESH_NODE_FEATURE_PROPERTY_MESHELEMENT:
        return mesh_node_feature_meshelement_array_replace_cb(id, len, value);
    case MESH_NODE_FEATURE_PROPERTY_MESHNETINDEX:
        return mesh_node_feature_meshnetindex_array_replace_cb(id, len, value);
    case MESH_NODE_FEATURE_PROPERTY_MESHAPPINDEX:
        return mesh_node_feature_meshappindex_array_replace_cb(id, len, value);
    default:
        return -1;
    }
}

static const struct hey_properties_callbacks props_cbs = {
    .write = service_mesh_node_feature_properties_write_cb,
    .array_add = service_mesh_node_feature_properties_array_add_cb,
    .array_del = service_mesh_node_feature_properties_array_del_cb,
    .array_replace = service_mesh_node_feature_properties_array_replace_cb,
};

static int mesh_node_feature_service_register(Iot__Service__MeshNodeFeature__Properties *props)
{
    uint8_t *buf;
    size_t buf_len;
    buf_len = iot__service__mesh_node_feature__properties__get_packed_size(props);
    if (buf_len == 0) {
        buf = NULL;
    } else {
        buf = malloc(buf_len);
        if (!buf) {
            return ENOMEM;
        }
        iot__service__mesh_node_feature__properties__pack(props, buf);
    }

    struct hey_schema_service schema_srv;
    hey_schema_service_init(&schema_srv);
    schema_srv.properties.iids = (struct hey_schema_property *)mesh_node_feature_properties_iids;
    schema_srv.properties.n_iid = PROP_IID_CNT;
    schema_srv.properties.value = buf;
    schema_srv.properties.len = buf_len;
    schema_srv.properties.cbs = &props_cbs;
    mesh_node_feature_service = hey_service_register(MESH_NODE_FEATURE_SERVICE, &schema_srv, NULL);
    if (NULL == mesh_node_feature_service) {
        free(buf);
        return errno;
    }

    free(buf);
    return 0;
}

/**
 * @brief update properties of mesh node feature service
 * @param count      [the number of properties to be updated]
 * @param enumerate  [the iid enumeration of properties to be updated]
 * @param properties [properties structure with updated content]
 * @return int32_t   [update result]
 */
int32_t mesh_node_feature_properties_update(size_t count, uint32_t *enumerate, struct mesh_node_feature_properties *properties)
{
    assert(count <= PROP_IID_CNT);
    if (NULL == properties) {
        return EINVAL;
    }

    int ret = 0;
    Iot__Service__MeshNodeFeature__Properties *props = NULL;
    uint8_t *buf = NULL;
    ArrayNode **meshelement = NULL;
    ArrayNode **meshnetindex = NULL;
    ArrayNode **meshappindex = NULL;
    props = malloc(sizeof(Iot__Service__MeshNodeFeature__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__mesh_node_feature__properties__init(props);
    if (properties->n_meshelement) {
        meshelement = calloc(properties->n_meshelement, sizeof(ArrayNode *));
        if (NULL == meshelement) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < properties->n_meshelement; i++) {
            meshelement[i] = malloc(sizeof(ArrayNode));
            if (NULL == meshelement[i]) {
                ret = ENOMEM;
                goto out;
            }
            array_node__init(meshelement[i]);
            meshelement[i]->id = properties->meshelement[i]->id;
            meshelement[i]->data.data = properties->meshelement[i]->data.data;
            meshelement[i]->data.len = properties->meshelement[i]->data.len;
        }
    }
    if (properties->n_meshnetindex) {
        meshnetindex = calloc(properties->n_meshnetindex, sizeof(ArrayNode *));
        if (NULL == meshnetindex) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < properties->n_meshnetindex; i++) {
            meshnetindex[i] = malloc(sizeof(ArrayNode));
            if (NULL == meshnetindex[i]) {
                ret = ENOMEM;
                goto out;
            }
            array_node__init(meshnetindex[i]);
            meshnetindex[i]->id = properties->meshnetindex[i]->id;
            meshnetindex[i]->data.data = properties->meshnetindex[i]->data.data;
            meshnetindex[i]->data.len = properties->meshnetindex[i]->data.len;
        }
    }
    if (properties->n_meshappindex) {
        meshappindex = calloc(properties->n_meshappindex, sizeof(ArrayNode *));
        if (NULL == meshappindex) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < properties->n_meshappindex; i++) {
            meshappindex[i] = malloc(sizeof(ArrayNode));
            if (NULL == meshappindex[i]) {
                ret = ENOMEM;
                goto out;
            }
            array_node__init(meshappindex[i]);
            meshappindex[i]->id = properties->meshappindex[i]->id;
            meshappindex[i]->data.data = properties->meshappindex[i]->data.data;
            meshappindex[i]->data.len = properties->meshappindex[i]->data.len;
        }
    }
    props->primaryelemaddr = properties->primaryelemaddr;
    props->n_meshelement = properties->n_meshelement;
    props->meshelement = meshelement;
    props->n_meshnetindex = properties->n_meshnetindex;
    props->meshnetindex = meshnetindex;
    props->n_meshappindex = properties->n_meshappindex;
    props->meshappindex = meshappindex;
    props->feature = properties->feature;

    size_t buf_len;
    buf_len = iot__service__mesh_node_feature__properties__get_packed_size(props);
    if (buf_len > 0) {
        buf = malloc(buf_len);
        if (NULL == buf) {
            ret = ENOMEM;
            goto out;
        }
        iot__service__mesh_node_feature__properties__pack(props, buf);
    }

    struct hey_properties_content content = {
        .n_iid = count,
        .iids = enumerate,
        .len = buf_len,
        .value = buf
    };
    ret = hey_properties_update(mesh_node_feature_service, &content);

out:
    free(props);
    free(buf);
    if (meshelement) {
        for (int i = 0; i < properties->n_meshelement; i++) {
            free(meshelement[i]);
        }
        free(meshelement);
    }
    if (meshnetindex) {
        for (int i = 0; i < properties->n_meshnetindex; i++) {
            free(meshnetindex[i]);
        }
        free(meshnetindex);
    }
    if (meshappindex) {
        for (int i = 0; i < properties->n_meshappindex; i++) {
            free(meshappindex[i]);
        }
        free(meshappindex);
    }
    return ret;
}

/**
 * @brief mesh node feature service initialization
 * @param properties [mesh node feature service properties]
 * @return int32_t   [initialize result]
*/
int32_t mesh_node_feature_service_init(const struct mesh_node_feature_properties *properties)
{
    int ret = 0;
    Iot__Service__MeshNodeFeature__Properties *props = NULL;
    ArrayNode **meshelement = NULL;
    ArrayNode **meshnetindex = NULL;
    ArrayNode **meshappindex = NULL;
    props = malloc(sizeof(Iot__Service__MeshNodeFeature__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__mesh_node_feature__properties__init(props);
    if (NULL != properties) {
        if (properties->n_meshelement) {
            meshelement = calloc(properties->n_meshelement, sizeof(ArrayNode *));
            if (NULL == meshelement) {
                ret = ENOMEM;
                goto out;
            }
            for (int i = 0; i < properties->n_meshelement; i++) {
                meshelement[i] = malloc(sizeof(ArrayNode));
                if (NULL == meshelement[i]) {
                    ret = ENOMEM;
                    goto out;
                }
                array_node__init(meshelement[i]);
                meshelement[i]->id = properties->meshelement[i]->id;
                meshelement[i]->data.data = properties->meshelement[i]->data.data;
                meshelement[i]->data.len = properties->meshelement[i]->data.len;
            }
        }
        if (properties->n_meshnetindex) {
            meshnetindex = calloc(properties->n_meshnetindex, sizeof(ArrayNode *));
            if (NULL == meshnetindex) {
                ret = ENOMEM;
                goto out;
            }
            for (int i = 0; i < properties->n_meshnetindex; i++) {
                meshnetindex[i] = malloc(sizeof(ArrayNode));
                if (NULL == meshnetindex[i]) {
                    ret = ENOMEM;
                    goto out;
                }
                array_node__init(meshnetindex[i]);
                meshnetindex[i]->id = properties->meshnetindex[i]->id;
                meshnetindex[i]->data.data = properties->meshnetindex[i]->data.data;
                meshnetindex[i]->data.len = properties->meshnetindex[i]->data.len;
            }
        }
        if (properties->n_meshappindex) {
            meshappindex = calloc(properties->n_meshappindex, sizeof(ArrayNode *));
            if (NULL == meshappindex) {
                ret = ENOMEM;
                goto out;
            }
            for (int i = 0; i < properties->n_meshappindex; i++) {
                meshappindex[i] = malloc(sizeof(ArrayNode));
                if (NULL == meshappindex[i]) {
                    ret = ENOMEM;
                    goto out;
                }
                array_node__init(meshappindex[i]);
                meshappindex[i]->id = properties->meshappindex[i]->id;
                meshappindex[i]->data.data = properties->meshappindex[i]->data.data;
                meshappindex[i]->data.len = properties->meshappindex[i]->data.len;
            }
        }
        props->primaryelemaddr = properties->primaryelemaddr;
        props->n_meshelement = properties->n_meshelement;
        props->meshelement = meshelement;
        props->n_meshnetindex = properties->n_meshnetindex;
        props->meshnetindex = meshnetindex;
        props->n_meshappindex = properties->n_meshappindex;
        props->meshappindex = meshappindex;
        props->feature = properties->feature;
    }

    ret = mesh_node_feature_service_register(props);

out:
    free(props);
    if (meshelement) {
        for (int i = 0; i < properties->n_meshelement; i++) {
            free(meshelement[i]);
        }
        free(meshelement);
    }
    if (meshnetindex) {
        for (int i = 0; i < properties->n_meshnetindex; i++) {
            free(meshnetindex[i]);
        }
        free(meshnetindex);
    }
    if (meshappindex) {
        for (int i = 0; i < properties->n_meshappindex; i++) {
            free(meshappindex[i]);
        }
        free(meshappindex);
    }
    return ret;
}
