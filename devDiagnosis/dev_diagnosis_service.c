/* Generated by the heythings model compiler.  DO NOT EDIT! */
/* Generated from: devDiagnosis.proto */

#include <errno.h>
#include <assert.h>
#include "devDiagnosis.pb-c.h"
#include "dev_diagnosis_service.h"
#include "hey/schema.h"

#define DEV_DIAGNOSIS_SERVICE (250 * 256)
#define PROP_IID_CNT 1
#define ACT_IID_CNT 2

static const struct hey_schema_property dev_diagnosis_properties_iids[] = {
    [0] = { .iid = 1, .type = HEY_PROPERTY_TYPE_GENERIC },
};
static struct hey_service *dev_diagnosis_service;

static int32_t action_upload_log_cmd_start_cb(void *cb_data, struct hey_action_ctx *ctx, size_t len, uint8_t *value, void **user_context)
{
    int32_t ret = -1;
    Iot__Service__DevDiagnosis__ActionInUploadLogCmd *upload_log_cmd = iot__service__dev_diagnosis__action_in_upload_log_cmd__unpack(NULL, len, value);
    if (NULL == upload_log_cmd) {
        hey_action_stop(ctx);
        return EINVAL;
    }

    struct dev_diagnosis_upload_log_cmd_in action = { 0 };
    ret = dev_diagnosis_upload_log_cmd_start_cb(ctx, &action, user_context);

    iot__service__dev_diagnosis__action_in_upload_log_cmd__free_unpacked(upload_log_cmd, NULL);
    return ret;
}

static void action_upload_log_cmd_stop_cb(void *cb_data, void *user_context)
{
    dev_diagnosis_upload_log_cmd_stop_cb(user_context);
}

/**
 * @brief upload log cmd action send message of dev diagnosis service
 * @param ctx      [action context]
 * @param action   [upload log cmd action out]
 * @return int32_t [action send result, 0 means success]
 */
int32_t dev_diagnosis_upload_log_cmd_send(struct hey_action_ctx *ctx, struct dev_diagnosis_upload_log_cmd_out *action)
{
    return hey_action_msg_send(ctx, 0, NULL);
}

/**
 * @brief upload log cmd action stop of dev diagnosis service
 * @param ctx      [action context]
 * @return int32_t [action stop result, 0 means success]
 */
int32_t dev_diagnosis_upload_log_cmd_stop(struct hey_action_ctx *ctx)
{
    return hey_action_stop(ctx);
}

static int32_t action_get_log_data_start_cb(void *cb_data, struct hey_action_ctx *ctx, size_t len, uint8_t *value, void **user_context)
{
    int32_t ret = -1;
    Iot__Service__DevDiagnosis__ActionInGetLogData *get_log_data = iot__service__dev_diagnosis__action_in_get_log_data__unpack(NULL, len, value);
    if (NULL == get_log_data) {
        hey_action_stop(ctx);
        return EINVAL;
    }

    struct dev_diagnosis_get_log_data_in action = { 0 };
    ret = dev_diagnosis_get_log_data_start_cb(ctx, &action, user_context);

    iot__service__dev_diagnosis__action_in_get_log_data__free_unpacked(get_log_data, NULL);
    return ret;
}

static void action_get_log_data_stop_cb(void *cb_data, void *user_context)
{
    dev_diagnosis_get_log_data_stop_cb(user_context);
}

/**
 * @brief get log data action send message of dev diagnosis service
 * @param ctx      [action context]
 * @param action   [get log data action out]
 * @return int32_t [action send result, 0 means success]
 */
int32_t dev_diagnosis_get_log_data_send(struct hey_action_ctx *ctx, struct dev_diagnosis_get_log_data_out *action)
{
    int32_t ret = 0;
    Iot__Service__DevDiagnosis__ActionOutGetLogData *out = NULL;
    uint8_t *buf = NULL;
    out = malloc(sizeof(Iot__Service__DevDiagnosis__ActionOutGetLogData));
    if (NULL == out) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__dev_diagnosis__action_out_get_log_data__init(out);
    out->logdata.data = action->logdata.data;
    out->logdata.len = action->logdata.len;
    size_t buf_len = iot__service__dev_diagnosis__action_out_get_log_data__get_packed_size(out);
    if (buf_len > 0) {
        buf = malloc(buf_len);
        if (NULL == buf) {
            ret = ENOMEM;
            goto out;
        }
    }
    iot__service__dev_diagnosis__action_out_get_log_data__pack(out, buf);
    ret = hey_action_msg_send(ctx, buf_len, buf);

out:
    free(out);
    free(buf);
    return ret;
}

/**
 * @brief get log data action stop of dev diagnosis service
 * @param ctx      [action context]
 * @return int32_t [action stop result, 0 means success]
 */
int32_t dev_diagnosis_get_log_data_stop(struct hey_action_ctx *ctx)
{
    return hey_action_stop(ctx);
}

static int32_t service_dev_diagnosis_properties_write_cb(void *cb_data, size_t n_iid, uint32_t *iids,
                                                         size_t len, uint8_t *value)
{
    Iot__Service__DevDiagnosis__Properties *props = iot__service__dev_diagnosis__properties__unpack(NULL, len, value);
    if (NULL == props) {
        return EINVAL;
    }

    int32_t ret = 0;
    struct dev_diagnosis_properties properties = { 0 };
    properties.enable = props->enable;
    ret = dev_diagnosis_properties_write_cb(n_iid, iids, &properties);

    iot__service__dev_diagnosis__properties__free_unpacked(props, NULL);
    return ret;
}

static const struct hey_properties_callbacks props_cbs = {
    .write = service_dev_diagnosis_properties_write_cb
};

static const struct hey_action_ctx_callbacks upload_log_cmd_cbs = {
    .start = action_upload_log_cmd_start_cb,
    .stop = action_upload_log_cmd_stop_cb
};

static const struct hey_action_ctx_callbacks get_log_data_cbs = {
    .start = action_get_log_data_start_cb,
    .stop = action_get_log_data_stop_cb
};

static const struct hey_action_entry actions_iids[] = {
    [0] = { .iid = DEV_DIAGNOSIS_ACTION_UPLOAD_LOG_CMD, .type = HEY_ACTION_ONCE, .cbs = &upload_log_cmd_cbs },
    [1] = { .iid = DEV_DIAGNOSIS_ACTION_GET_LOG_DATA, .type = HEY_ACTION_IN_ONCE_OUT_STREAM, .cbs = &get_log_data_cbs },
};

static int dev_diagnosis_service_register(Iot__Service__DevDiagnosis__Properties *props)
{
    uint8_t *buf;
    size_t buf_len;
    buf_len = iot__service__dev_diagnosis__properties__get_packed_size(props);
    if (buf_len == 0) {
        buf = NULL;
    } else {
        buf = malloc(buf_len);
        if (!buf) {
            return ENOMEM;
        }
        iot__service__dev_diagnosis__properties__pack(props, buf);
    }

    struct hey_schema_service schema_srv;
    hey_schema_service_init(&schema_srv);
    schema_srv.properties.iids = (struct hey_schema_property *)dev_diagnosis_properties_iids;
    schema_srv.properties.n_iid = PROP_IID_CNT;
    schema_srv.properties.value = buf;
    schema_srv.properties.len = buf_len;
    schema_srv.properties.cbs = &props_cbs;
    schema_srv.actions.iids = (struct hey_action_entry *)actions_iids;
    schema_srv.actions.n_iid = ACT_IID_CNT;
    dev_diagnosis_service = hey_service_register(DEV_DIAGNOSIS_SERVICE, &schema_srv, NULL);
    if (NULL == dev_diagnosis_service) {
        free(buf);
        return errno;
    }

    free(buf);
    return 0;
}

/**
 * @brief update properties of dev diagnosis service
 * @param count      [the number of properties to be updated]
 * @param enumerate  [the iid enumeration of properties to be updated]
 * @param properties [properties structure with updated content]
 * @return int32_t   [update result]
 */
int32_t dev_diagnosis_properties_update(size_t count, uint32_t *enumerate, struct dev_diagnosis_properties *properties)
{
    assert(count <= PROP_IID_CNT);
    if (NULL == properties) {
        return EINVAL;
    }

    int ret = 0;
    Iot__Service__DevDiagnosis__Properties *props = NULL;
    uint8_t *buf = NULL;
    props = malloc(sizeof(Iot__Service__DevDiagnosis__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__dev_diagnosis__properties__init(props);
    props->enable = properties->enable;

    size_t buf_len;
    buf_len = iot__service__dev_diagnosis__properties__get_packed_size(props);
    if (buf_len > 0) {
        buf = malloc(buf_len);
        if (NULL == buf) {
            ret = ENOMEM;
            goto out;
        }
        iot__service__dev_diagnosis__properties__pack(props, buf);
    }

    struct hey_properties_content content = {
        .n_iid = count,
        .iids = enumerate,
        .len = buf_len,
        .value = buf
    };
    ret = hey_properties_update(dev_diagnosis_service, &content);

out:
    free(props);
    free(buf);
    return ret;
}

/**
 * @brief dev diagnosis service initialization
 * @param properties [dev diagnosis service properties]
 * @return int32_t   [initialize result]
*/
int32_t dev_diagnosis_service_init(const struct dev_diagnosis_properties *properties)
{
    int ret = 0;
    Iot__Service__DevDiagnosis__Properties *props = NULL;
    props = malloc(sizeof(Iot__Service__DevDiagnosis__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__dev_diagnosis__properties__init(props);
    if (NULL != properties) {
        props->enable = properties->enable;
    }

    ret = dev_diagnosis_service_register(props);

out:
    free(props);
    return ret;
}
