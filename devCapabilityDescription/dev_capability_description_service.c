/* Generated by the heythings model compiler.  DO NOT EDIT! */
/* Generated from: devCapabilityDescription.proto */

#include <errno.h>
#include <assert.h>
#include "devCapabilityDescription.pb-c.h"
#include "dev_capability_description_service.h"
#include "hey/schema.h"

#define DEV_CAPABILITY_DESCRIPTION_SERVICE 
#define PROP_IID_CNT 1

static const struct hey_schema_property dev_capability_description_properties_iids[] = {
    [0] = { .iid = 1, .type = HEY_PROPERTY_TYPE_ARRAY },
};
static struct hey_service *dev_capability_description_service;

static Iot__Service__DevCapabilityDescription__Services *_dev_capability_description_services = NULL;
struct dev_capability_description_services *dev_capability_description_services_unpack(const uint8_t *data, size_t len)
{
    _dev_capability_description_services = iot__service__dev_capability_description__services__unpack(NULL, len, data);
    if (NULL == _dev_capability_description_services) {
        return NULL;
    }
    struct dev_capability_description_services *hey_struct = malloc(sizeof(struct dev_capability_description_services));
    if (NULL == hey_struct) {
        goto out1;
    }
    hey_struct->siid = _dev_capability_description_services->siid;
    hey_struct->n_iid = _dev_capability_description_services->n_iid;
    hey_struct->iid = iid;
    return hey_struct;
out1:
    iot__service__dev_capability_description__services__free_unpacked(_dev_capability_description_services, NULL);
    return NULL;
}

void dev_capability_description_services_free_unpacked(struct dev_capability_description_services *data)
{
    assert(NULL != data);
    iot__service__dev_capability_description__services__free_unpacked(_dev_capability_description_services, NULL);
    free(data);
}

int32_t dev_capability_description_services_get_packed_size(const struct dev_capability_description_services *data)
{
    assert(data);
    int ret = 0;
    Iot__Service__DevCapabilityDescription__Services *pb_struct = malloc(sizeof(Iot__Service__DevCapabilityDescription__Services));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__dev_capability_description__services__init(pb_struct);
    pb_struct->siid = data->siid;
    pb_struct->n_iid = data->n_iid;
    pb_struct->iid = iid;
    ret = iot__service__dev_capability_description__services__get_packed_size(pb_struct);
    free(pb_struct);
    return ret;
}

int32_t dev_capability_description_services_pack(const struct dev_capability_description_services *data, uint8_t *out)
{
    assert(data);
    int ret = 0;
    Iot__Service__DevCapabilityDescription__Services *pb_struct = malloc(sizeof(Iot__Service__DevCapabilityDescription__Services));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__dev_capability_description__services__init(pb_struct);
    pb_struct->siid = data->siid;
    pb_struct->n_iid = data->n_iid;
    pb_struct->iid = iid;
    ret = iot__service__dev_capability_description__services__pack(pb_struct, out);
    free(pb_struct);
    return ret;
}

static int32_t service_dev_capability_description_properties_write_cb(void *cb_data, size_t n_iid, uint32_t *iids,
                                                                      size_t len, uint8_t *value)
{
    Iot__Service__DevCapabilityDescription__Properties *props = iot__service__dev_capability_description__properties__unpack(NULL, len, value);
    if (NULL == props) {
        return EINVAL;
    }

    int32_t ret = 0;
    struct dev_capability_description_properties properties = { 0 };
    struct hey_array_node **services = NULL;
    if (props->n_services) {
        services = calloc(props->n_services, sizeof(struct hey_array_node *));
        if (services == NULL) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < props->n_services; i++) {
            services[i] = malloc(sizeof(struct hey_array_node));
            if (services[i] == NULL) {
                ret = ENOMEM;
                goto out;
            }
            services[i]->id = props->services[i]->id;
            services[i]->data.data = props->services[i]->data.data;
            services[i]->data.len = props->services[i]->data.len;
        }
        properties.n_services = props->n_services;
        properties.services = services;
    }
    ret = dev_capability_description_properties_write_cb(n_iid, iids, &properties);

out:
    if (services) {
        for (int i = 0; i < props->n_services; i++) {
            free(services[i]);
        }
        free(services);
    }
    iot__service__dev_capability_description__properties__free_unpacked(props, NULL);
    return ret;
}

static int32_t service_dev_capability_description_properties_array_add_cb(void *cb_data, uint32_t iid, size_t len, uint8_t *value)
{
    switch(iid) {
    case DEV_CAPABILITY_DESCRIPTION_PROPERTY_SERVICES:
        return dev_capability_description_services_array_add_cb(len, value);
    default:
        return -1;
    }
}

static int32_t service_dev_capability_description_properties_array_del_cb(void *cb_data, uint32_t iid, size_t n_id, uint32_t *ids)
{
    switch(iid) {
    case DEV_CAPABILITY_DESCRIPTION_PROPERTY_SERVICES:
        return dev_capability_description_services_array_del_cb(n_id, ids);
    default:
        return -1;
    }
}

static int32_t service_dev_capability_description_properties_array_replace_cb(void *cb_data, uint32_t iid, uint32_t id, size_t len, uint8_t *value)
{
    switch(iid) {
    case DEV_CAPABILITY_DESCRIPTION_PROPERTY_SERVICES:
        return dev_capability_description_services_array_replace_cb(id, len, value);
    default:
        return -1;
    }
}

static const struct hey_properties_callbacks props_cbs = {
    .write = service_dev_capability_description_properties_write_cb,
    .array_add = service_dev_capability_description_properties_array_add_cb,
    .array_del = service_dev_capability_description_properties_array_del_cb,
    .array_replace = service_dev_capability_description_properties_array_replace_cb,
};

static int dev_capability_description_service_register(Iot__Service__DevCapabilityDescription__Properties *props)
{
    uint8_t *buf;
    size_t buf_len;
    buf_len = iot__service__dev_capability_description__properties__get_packed_size(props);
    if (buf_len == 0) {
        buf = NULL;
    } else {
        buf = malloc(buf_len);
        if (!buf) {
            return ENOMEM;
        }
        iot__service__dev_capability_description__properties__pack(props, buf);
    }

    struct hey_schema_service schema_srv;
    hey_schema_service_init(&schema_srv);
    schema_srv.properties.iids = (struct hey_schema_property *)dev_capability_description_properties_iids;
    schema_srv.properties.n_iid = PROP_IID_CNT;
    schema_srv.properties.value = buf;
    schema_srv.properties.len = buf_len;
    schema_srv.properties.cbs = &props_cbs;
    dev_capability_description_service = hey_service_register(DEV_CAPABILITY_DESCRIPTION_SERVICE, &schema_srv, NULL);
    if (NULL == dev_capability_description_service) {
        free(buf);
        return errno;
    }

    free(buf);
    return 0;
}

/**
 * @brief update properties of dev capability description service
 * @param count      [the number of properties to be updated]
 * @param enumerate  [the iid enumeration of properties to be updated]
 * @param properties [properties structure with updated content]
 * @return int32_t   [update result]
 */
int32_t dev_capability_description_properties_update(size_t count, uint32_t *enumerate, struct dev_capability_description_properties *properties)
{
    assert(count <= PROP_IID_CNT);
    if (NULL == properties) {
        return EINVAL;
    }

    int ret = 0;
    Iot__Service__DevCapabilityDescription__Properties *props = NULL;
    uint8_t *buf = NULL;
    ArrayNode **services = NULL;
    props = malloc(sizeof(Iot__Service__DevCapabilityDescription__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__dev_capability_description__properties__init(props);
    if (properties->n_services) {
        services = calloc(properties->n_services, sizeof(ArrayNode *));
        if (NULL == services) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < properties->n_services; i++) {
            services[i] = malloc(sizeof(ArrayNode));
            if (NULL == services[i]) {
                ret = ENOMEM;
                goto out;
            }
            array_node__init(services[i]);
            services[i]->id = properties->services[i]->id;
            services[i]->data.data = properties->services[i]->data.data;
            services[i]->data.len = properties->services[i]->data.len;
        }
    }
    props->n_services = properties->n_services;
    props->services = services;

    size_t buf_len;
    buf_len = iot__service__dev_capability_description__properties__get_packed_size(props);
    if (buf_len > 0) {
        buf = malloc(buf_len);
        if (NULL == buf) {
            ret = ENOMEM;
            goto out;
        }
        iot__service__dev_capability_description__properties__pack(props, buf);
    }

    struct hey_properties_content content = {
        .n_iid = count,
        .iids = enumerate,
        .len = buf_len,
        .value = buf
    };
    ret = hey_properties_update(dev_capability_description_service, &content);

out:
    free(props);
    free(buf);
    if (services) {
        for (int i = 0; i < properties->n_services; i++) {
            free(services[i]);
        }
        free(services);
    }
    return ret;
}

/**
 * @brief dev capability description service initialization
 * @param properties [dev capability description service properties]
 * @return int32_t   [initialize result]
*/
int32_t dev_capability_description_service_init(const struct dev_capability_description_properties *properties)
{
    int ret = 0;
    Iot__Service__DevCapabilityDescription__Properties *props = NULL;
    ArrayNode **services = NULL;
    props = malloc(sizeof(Iot__Service__DevCapabilityDescription__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__dev_capability_description__properties__init(props);
    if (NULL != properties) {
        if (properties->n_services) {
            services = calloc(properties->n_services, sizeof(ArrayNode *));
            if (NULL == services) {
                ret = ENOMEM;
                goto out;
            }
            for (int i = 0; i < properties->n_services; i++) {
                services[i] = malloc(sizeof(ArrayNode));
                if (NULL == services[i]) {
                    ret = ENOMEM;
                    goto out;
                }
                array_node__init(services[i]);
                services[i]->id = properties->services[i]->id;
                services[i]->data.data = properties->services[i]->data.data;
                services[i]->data.len = properties->services[i]->data.len;
            }
        }
        props->n_services = properties->n_services;
        props->services = services;
    }

    ret = dev_capability_description_service_register(props);

out:
    free(props);
    if (services) {
        for (int i = 0; i < properties->n_services; i++) {
            free(services[i]);
        }
        free(services);
    }
    return ret;
}
