/* Generated by the heythings model compiler.  DO NOT EDIT! */
/* Generated from: acAssistant.proto */

#include <errno.h>
#include <assert.h>
#include "acAssistant.pb-c.h"
#include "ac_assistant_service.h"
#include "hey/schema.h"

#define AC_ASSISTANT_SERVICE (44 * 256)
#define PROP_IID_CNT 15
#define ACT_IID_CNT 1

static const struct hey_schema_property ac_assistant_properties_iids[] = {
    [0] = { .iid = 1, .type = HEY_PROPERTY_TYPE_GENERIC },
    [1] = { .iid = 2, .type = HEY_PROPERTY_TYPE_GENERIC },
    [2] = { .iid = 3, .type = HEY_PROPERTY_TYPE_GENERIC },
    [3] = { .iid = 4, .type = HEY_PROPERTY_TYPE_GENERIC },
    [4] = { .iid = 5, .type = HEY_PROPERTY_TYPE_GENERIC },
    [5] = { .iid = 6, .type = HEY_PROPERTY_TYPE_GENERIC },
    [6] = { .iid = 7, .type = HEY_PROPERTY_TYPE_GENERIC },
    [7] = { .iid = 8, .type = HEY_PROPERTY_TYPE_GENERIC },
    [8] = { .iid = 9, .type = HEY_PROPERTY_TYPE_GENERIC },
    [9] = { .iid = 10, .type = HEY_PROPERTY_TYPE_GENERIC },
    [10] = { .iid = 11, .type = HEY_PROPERTY_TYPE_GENERIC },
    [11] = { .iid = 12, .type = HEY_PROPERTY_TYPE_GENERIC },
    [12] = { .iid = 13, .type = HEY_PROPERTY_TYPE_GENERIC },
    [13] = { .iid = 14, .type = HEY_PROPERTY_TYPE_GENERIC },
    [14] = { .iid = 15, .type = HEY_PROPERTY_TYPE_ARRAY },
};
static struct hey_service *ac_assistant_service;

static int32_t action_issuing_ir_command_start_cb(void *cb_data, struct hey_action_ctx *ctx, size_t len, uint8_t *value, void **user_context)
{
    int32_t ret = -1;
    Iot__Service__AcAssistant__ActionInIssuingIrCommand *issuing_ir_command = iot__service__ac_assistant__action_in_issuing_ir_command__unpack(NULL, len, value);
    if (NULL == issuing_ir_command) {
        hey_action_stop(ctx);
        return EINVAL;
    }

    struct ac_assistant_issuing_ir_command_in action = { 0 };
    action.ircode.data = issuing_ir_command->ircode.data;
    action.ircode.len = issuing_ir_command->ircode.len;
    ret = ac_assistant_issuing_ir_command_start_cb(ctx, &action, user_context);

    iot__service__ac_assistant__action_in_issuing_ir_command__free_unpacked(issuing_ir_command, NULL);
    return ret;
}

static void action_issuing_ir_command_stop_cb(void *cb_data, void *user_context)
{
    ac_assistant_issuing_ir_command_stop_cb(user_context);
}

/**
 * @brief issuing ir command action send message of ac assistant service
 * @param ctx      [action context]
 * @param action   [issuing ir command action out]
 * @return int32_t [action send result, 0 means success]
 */
int32_t ac_assistant_issuing_ir_command_send(struct hey_action_ctx *ctx, struct ac_assistant_issuing_ir_command_out *action)
{
    return hey_action_msg_send(ctx, 0, NULL);
}

/**
 * @brief issuing ir command action stop of ac assistant service
 * @param ctx      [action context]
 * @return int32_t [action stop result, 0 means success]
 */
int32_t ac_assistant_issuing_ir_command_stop(struct hey_action_ctx *ctx)
{
    return hey_action_stop(ctx);
}

static Iot__Service__AcAssistant__SleepCurveModeDefinition *_ac_assistant_sleep_curve_mode_definition = NULL;
struct ac_assistant_sleep_curve_mode_definition *ac_assistant_sleep_curve_mode_definition_unpack(const uint8_t *data, size_t len)
{
    _ac_assistant_sleep_curve_mode_definition = iot__service__ac_assistant__sleep_curve_mode_definition__unpack(NULL, len, data);
    if (NULL == _ac_assistant_sleep_curve_mode_definition) {
        return NULL;
    }
    struct ac_assistant_sleep_curve_mode_definition *hey_struct = malloc(sizeof(struct ac_assistant_sleep_curve_mode_definition));
    if (NULL == hey_struct) {
        goto out1;
    }
    struct sleep_curve_mode_definition_mode_definition **modedefinition = NULL;
    modedefinition = calloc(_ac_assistant_sleep_curve_mode_definition->n_modedefinition, sizeof(struct sleep_curve_mode_definition_mode_definition *));
    if (NULL == modedefinition) {
        goto out2;
    }
    for (int i = 0; i < _ac_assistant_sleep_curve_mode_definition->n_modedefinition; i++) {
        modedefinition[i] = malloc(sizeof(struct sleep_curve_mode_definition_mode_definition));
        if (NULL == modedefinition[i]) {
            goto out3;
        }
        modedefinition[i]->hour = _ac_assistant_sleep_curve_mode_definition->modedefinition[i]->hour;
        modedefinition[i]->min = _ac_assistant_sleep_curve_mode_definition->modedefinition[i]->min;
        modedefinition[i]->n_config = _ac_assistant_sleep_curve_mode_definition->modedefinition[i]->n_config;
        modedefinition[i]->config = calloc(modedefinition[i]->n_config, sizeof(*modedefinition[i]->config));
        if (NULL == modedefinition[i]->config) {
            goto out4;
        }
        for (int j = 0; j < modedefinition[i]->n_config; j++) {
            modedefinition[i]->config[j] = malloc(sizeof(*modedefinition[i]->config[j]));
            if (NULL == modedefinition[i]->config[j]) {
                goto out4;
            }
            modedefinition[i]->config[j]->siid = _ac_assistant_sleep_curve_mode_definition->modedefinition[i]->config[j]->siid;
            modedefinition[i]->config[j]->n_iid = _ac_assistant_sleep_curve_mode_definition->modedefinition[i]->config[j]->n_iid;
            modedefinition[i]->config[j]->iid = _ac_assistant_sleep_curve_mode_definition->modedefinition[i]->config[j]->iid;
            modedefinition[i]->config[j]->data.data = _ac_assistant_sleep_curve_mode_definition->modedefinition[i]->config[j]->data.data;
            modedefinition[i]->config[j]->data.len = _ac_assistant_sleep_curve_mode_definition->modedefinition[i]->config[j]->data.len;
        }
    }
    hey_struct->mode = _ac_assistant_sleep_curve_mode_definition->mode;
    hey_struct->modename = _ac_assistant_sleep_curve_mode_definition->modename;
    hey_struct->n_modedefinition = _ac_assistant_sleep_curve_mode_definition->n_modedefinition;
    hey_struct->modedefinition = modedefinition;
    return hey_struct;
out4:
    for (int i = 0; i < _ac_assistant_sleep_curve_mode_definition->n_modedefinition; i++) {
        for (int j = 0; j < modedefinition[i]->n_config; j++) {
            if (NULL == modedefinition[i]->config[j])
                break;
            free(modedefinition[i]->config[j]);
        }
        free(modedefinition[i]->config);
    }
out3:
    for (int i = 0; i < _ac_assistant_sleep_curve_mode_definition->n_modedefinition; i++) {
        if (NULL == modedefinition[i])
            break;
        free(modedefinition[i]);
    }
    free(modedefinition);
out2:
    free(hey_struct);
out1:
    iot__service__ac_assistant__sleep_curve_mode_definition__free_unpacked(_ac_assistant_sleep_curve_mode_definition, NULL);
    return NULL;
}

void ac_assistant_sleep_curve_mode_definition_free_unpacked(struct ac_assistant_sleep_curve_mode_definition *data)
{
    assert(NULL != data);
    iot__service__ac_assistant__sleep_curve_mode_definition__free_unpacked(_ac_assistant_sleep_curve_mode_definition, NULL);
    for (int i = 0; i < data->n_modedefinition; i++) {
        for (int j = 0; j < data->modedefinition[i]->n_config; j++) {
            free(data->modedefinition[i]->config[j]);
        }
        free(data->modedefinition[i]->config);
        free(data->modedefinition[i]);
    }
    free(data->modedefinition);
    free(data);
}

int32_t ac_assistant_sleep_curve_mode_definition_get_packed_size(const struct ac_assistant_sleep_curve_mode_definition *data)
{
    assert(data);
    int ret = 0;
    Iot__Service__AcAssistant__SleepCurveModeDefinition *pb_struct = malloc(sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__ac_assistant__sleep_curve_mode_definition__init(pb_struct);
    Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition **modedefinition = NULL;
    if (data->n_modedefinition) {
        modedefinition = calloc(data->n_modedefinition, sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition *));
        if (NULL == modedefinition) {
            ret = -1;
            goto out;
        }
        for (int i = 0; i < data->n_modedefinition; i++) {
            modedefinition[i] = malloc(sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition));
            if (NULL == modedefinition[i]) {
                ret = -1;
                goto out;
            }
            iot__service__ac_assistant__sleep_curve_mode_definition__mode_definition__init(modedefinition[i]);
            modedefinition[i]->hour = data->modedefinition[i]->hour;
            modedefinition[i]->min = data->modedefinition[i]->min;
            modedefinition[i]->n_config = data->modedefinition[i]->n_config;
            if (modedefinition[i]->n_config) {
                modedefinition[i]->config = calloc(modedefinition[i]->n_config, sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition__Config *));
                if (NULL == modedefinition[i]->config) {
                    ret = -1;
                    goto out;
                }
                for (int j = 0; j < data->n_modedefinition; j++) {
                    modedefinition[i]->config[j] = malloc(sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition__Config));
                    if (NULL == modedefinition[i]->config[j]) {
                        ret = -1;
                        goto out1;
                    }
                    iot__service__ac_assistant__sleep_curve_mode_definition__mode_definition__config__init(modedefinition[i]->config[j]);
                    modedefinition[i]->config[j]->siid = data->modedefinition[i]->config[j]->siid;
                    modedefinition[i]->config[j]->n_iid = data->modedefinition[i]->config[j]->n_iid;
                    modedefinition[i]->config[j]->iid = data->modedefinition[i]->config[j]->iid;
                    modedefinition[i]->config[j]->data.data = data->modedefinition[i]->config[j]->data.data;
                    modedefinition[i]->config[j]->data.len = data->modedefinition[i]->config[j]->data.len;
                }
            }
        }
    }
    pb_struct->mode = data->mode;
    pb_struct->modename = data->modename;
    pb_struct->n_modedefinition = data->n_modedefinition;
    pb_struct->modedefinition = modedefinition;
    ret = iot__service__ac_assistant__sleep_curve_mode_definition__get_packed_size(pb_struct);
out1:
    for (int i = 0; i < data->n_modedefinition; i++) {
        for (int j = 0; j < modedefinition[i]->n_config; j++) {
            if (modedefinition[i]->config[j] == NULL)
                break;
            free(modedefinition[i]->config[j]);
        }
        free(modedefinition[i]->config);
    }
out:
    for (int i = 0; i < data->n_modedefinition; i++) {
        if (modedefinition[i] == NULL)
            break;
        free(modedefinition[i]);
    }
    free(modedefinition);
    free(pb_struct);
    return ret;
}

int32_t ac_assistant_sleep_curve_mode_definition_pack(const struct ac_assistant_sleep_curve_mode_definition *data, uint8_t *out)
{
    assert(data);
    int ret = 0;
    Iot__Service__AcAssistant__SleepCurveModeDefinition *pb_struct = malloc(sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition));
    if (pb_struct == NULL) {
        return -1;
    }
    iot__service__ac_assistant__sleep_curve_mode_definition__init(pb_struct);
    Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition **modedefinition = NULL;
    if (data->n_modedefinition) {
        modedefinition = calloc(data->n_modedefinition, sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition *));
        if (NULL == modedefinition) {
            ret = -1;
            goto out;
        }
        for (int i = 0; i < data->n_modedefinition; i++) {
            modedefinition[i] = malloc(sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition));
            if (NULL == modedefinition[i]) {
                ret = -1;
                goto out;
            }
            iot__service__ac_assistant__sleep_curve_mode_definition__mode_definition__init(modedefinition[i]);
            modedefinition[i]->hour = data->modedefinition[i]->hour;
            modedefinition[i]->min = data->modedefinition[i]->min;
            modedefinition[i]->n_config = data->modedefinition[i]->n_config;
            if (modedefinition[i]->n_config) {
                modedefinition[i]->config = calloc(modedefinition[i]->n_config, sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition__Config *));
                if (NULL == modedefinition[i]->config) {
                    ret = -1;
                    goto out;
                }
                for (int j = 0; j < data->n_modedefinition; j++) {
                    modedefinition[i]->config[j] = malloc(sizeof(Iot__Service__AcAssistant__SleepCurveModeDefinition__ModeDefinition__Config));
                    if (NULL == modedefinition[i]->config[j]) {
                        ret = -1;
                        goto out1;
                    }
                    iot__service__ac_assistant__sleep_curve_mode_definition__mode_definition__config__init(modedefinition[i]->config[j]);
                    modedefinition[i]->config[j]->siid = data->modedefinition[i]->config[j]->siid;
                    modedefinition[i]->config[j]->n_iid = data->modedefinition[i]->config[j]->n_iid;
                    modedefinition[i]->config[j]->iid = data->modedefinition[i]->config[j]->iid;
                    modedefinition[i]->config[j]->data.data = data->modedefinition[i]->config[j]->data.data;
                    modedefinition[i]->config[j]->data.len = data->modedefinition[i]->config[j]->data.len;
                }
            }
        }
    }
    pb_struct->mode = data->mode;
    pb_struct->modename = data->modename;
    pb_struct->n_modedefinition = data->n_modedefinition;
    pb_struct->modedefinition = modedefinition;
    ret = iot__service__ac_assistant__sleep_curve_mode_definition__pack(pb_struct, out);
out1:
    for (int i = 0; i < data->n_modedefinition; i++) {
        for (int j = 0; j < modedefinition[i]->n_config; j++) {
            if (modedefinition[i]->config[j] == NULL)
                break;
            free(modedefinition[i]->config[j]);
        }
        free(modedefinition[i]->config);
    }
out:
    for (int i = 0; i < data->n_modedefinition; i++) {
        if (modedefinition[i] == NULL)
            break;
        free(modedefinition[i]);
    }
    free(modedefinition);
    free(pb_struct);
    return ret;
}

static int32_t service_ac_assistant_properties_write_cb(void *cb_data, size_t n_iid, uint32_t *iids,
                                                        size_t len, uint8_t *value)
{
    Iot__Service__AcAssistant__Properties *props = iot__service__ac_assistant__properties__unpack(NULL, len, value);
    if (NULL == props) {
        return EINVAL;
    }

    int32_t ret = 0;
    struct ac_assistant_properties properties = { 0 };
    struct hey_array_node **sleepcurvemodedefinition = NULL;
    properties.power = props->power;
    properties.acmode = props->acmode;
    properties.eco = props->eco;
    properties.sleep = props->sleep;
    properties.rapidmode = props->rapidmode;
    properties.electricheating = props->electricheating;
    properties.targettemperature = props->targettemperature;
    properties.displayingtempunit = props->displayingtempunit;
    properties.fanmode = props->fanmode;
    properties.windspeed = props->windspeed;
    properties.horizontaldirection = props->horizontaldirection;
    properties.verticaldirection = props->verticaldirection;
    properties.sleepcurveenabled = props->sleepcurveenabled;
    properties.sleepcurvemode = props->sleepcurvemode;
    if (props->n_sleepcurvemodedefinition) {
        sleepcurvemodedefinition = calloc(props->n_sleepcurvemodedefinition, sizeof(struct hey_array_node *));
        if (sleepcurvemodedefinition == NULL) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < props->n_sleepcurvemodedefinition; i++) {
            sleepcurvemodedefinition[i] = malloc(sizeof(struct hey_array_node));
            if (sleepcurvemodedefinition[i] == NULL) {
                ret = ENOMEM;
                goto out;
            }
            sleepcurvemodedefinition[i]->id = props->sleepcurvemodedefinition[i]->id;
            sleepcurvemodedefinition[i]->data.data = props->sleepcurvemodedefinition[i]->data.data;
            sleepcurvemodedefinition[i]->data.len = props->sleepcurvemodedefinition[i]->data.len;
        }
        properties.n_sleepcurvemodedefinition = props->n_sleepcurvemodedefinition;
        properties.sleepcurvemodedefinition = sleepcurvemodedefinition;
    }
    ret = ac_assistant_properties_write_cb(n_iid, iids, &properties);

out:
    if (sleepcurvemodedefinition) {
        for (int i = 0; i < props->n_sleepcurvemodedefinition; i++) {
            free(sleepcurvemodedefinition[i]);
        }
        free(sleepcurvemodedefinition);
    }
    iot__service__ac_assistant__properties__free_unpacked(props, NULL);
    return ret;
}

static int32_t service_ac_assistant_properties_array_add_cb(void *cb_data, uint32_t iid, size_t len, uint8_t *value)
{
    switch(iid) {
    case AC_ASSISTANT_PROPERTY_SLEEPCURVEMODEDEFINITION:
        return ac_assistant_sleepcurvemodedefinition_array_add_cb(len, value);
    default:
        return -1;
    }
}

static int32_t service_ac_assistant_properties_array_del_cb(void *cb_data, uint32_t iid, size_t n_id, uint32_t *ids)
{
    switch(iid) {
    case AC_ASSISTANT_PROPERTY_SLEEPCURVEMODEDEFINITION:
        return ac_assistant_sleepcurvemodedefinition_array_del_cb(n_id, ids);
    default:
        return -1;
    }
}

static int32_t service_ac_assistant_properties_array_replace_cb(void *cb_data, uint32_t iid, uint32_t id, size_t len, uint8_t *value)
{
    switch(iid) {
    case AC_ASSISTANT_PROPERTY_SLEEPCURVEMODEDEFINITION:
        return ac_assistant_sleepcurvemodedefinition_array_replace_cb(id, len, value);
    default:
        return -1;
    }
}

static const struct hey_properties_callbacks props_cbs = {
    .write = service_ac_assistant_properties_write_cb,
    .array_add = service_ac_assistant_properties_array_add_cb,
    .array_del = service_ac_assistant_properties_array_del_cb,
    .array_replace = service_ac_assistant_properties_array_replace_cb,
};

static const struct hey_action_ctx_callbacks issuing_ir_command_cbs = {
    .start = action_issuing_ir_command_start_cb,
    .stop = action_issuing_ir_command_stop_cb
};

static const struct hey_action_entry actions_iids[] = {
    [0] = { .iid = AC_ASSISTANT_ACTION_ISSUING_IR_COMMAND, .type = HEY_ACTION_ONCE, .cbs = &issuing_ir_command_cbs },
};

static int ac_assistant_service_register(Iot__Service__AcAssistant__Properties *props)
{
    uint8_t *buf;
    size_t buf_len;
    buf_len = iot__service__ac_assistant__properties__get_packed_size(props);
    if (buf_len == 0) {
        buf = NULL;
    } else {
        buf = malloc(buf_len);
        if (!buf) {
            return ENOMEM;
        }
        iot__service__ac_assistant__properties__pack(props, buf);
    }

    struct hey_schema_service schema_srv;
    hey_schema_service_init(&schema_srv);
    schema_srv.properties.iids = (struct hey_schema_property *)ac_assistant_properties_iids;
    schema_srv.properties.n_iid = PROP_IID_CNT;
    schema_srv.properties.value = buf;
    schema_srv.properties.len = buf_len;
    schema_srv.properties.cbs = &props_cbs;
    schema_srv.actions.iids = (struct hey_action_entry *)actions_iids;
    schema_srv.actions.n_iid = ACT_IID_CNT;
    ac_assistant_service = hey_service_register(AC_ASSISTANT_SERVICE, &schema_srv, NULL);
    if (NULL == ac_assistant_service) {
        free(buf);
        return errno;
    }

    free(buf);
    return 0;
}

/**
 * @brief update properties of ac assistant service
 * @param count      [the number of properties to be updated]
 * @param enumerate  [the iid enumeration of properties to be updated]
 * @param properties [properties structure with updated content]
 * @return int32_t   [update result]
 */
int32_t ac_assistant_properties_update(size_t count, uint32_t *enumerate, struct ac_assistant_properties *properties)
{
    assert(count <= PROP_IID_CNT);
    if (NULL == properties) {
        return EINVAL;
    }

    int ret = 0;
    Iot__Service__AcAssistant__Properties *props = NULL;
    uint8_t *buf = NULL;
    ArrayNode **sleepcurvemodedefinition = NULL;
    props = malloc(sizeof(Iot__Service__AcAssistant__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__ac_assistant__properties__init(props);
    if (properties->n_sleepcurvemodedefinition) {
        sleepcurvemodedefinition = calloc(properties->n_sleepcurvemodedefinition, sizeof(ArrayNode *));
        if (NULL == sleepcurvemodedefinition) {
            ret = ENOMEM;
            goto out;
        }
        for (int i = 0; i < properties->n_sleepcurvemodedefinition; i++) {
            sleepcurvemodedefinition[i] = malloc(sizeof(ArrayNode));
            if (NULL == sleepcurvemodedefinition[i]) {
                ret = ENOMEM;
                goto out;
            }
            array_node__init(sleepcurvemodedefinition[i]);
            sleepcurvemodedefinition[i]->id = properties->sleepcurvemodedefinition[i]->id;
            sleepcurvemodedefinition[i]->data.data = properties->sleepcurvemodedefinition[i]->data.data;
            sleepcurvemodedefinition[i]->data.len = properties->sleepcurvemodedefinition[i]->data.len;
        }
    }
    props->power = properties->power;
    props->acmode = properties->acmode;
    props->eco = properties->eco;
    props->sleep = properties->sleep;
    props->rapidmode = properties->rapidmode;
    props->electricheating = properties->electricheating;
    props->targettemperature = properties->targettemperature;
    props->displayingtempunit = properties->displayingtempunit;
    props->fanmode = properties->fanmode;
    props->windspeed = properties->windspeed;
    props->horizontaldirection = properties->horizontaldirection;
    props->verticaldirection = properties->verticaldirection;
    props->sleepcurveenabled = properties->sleepcurveenabled;
    props->sleepcurvemode = properties->sleepcurvemode;
    props->n_sleepcurvemodedefinition = properties->n_sleepcurvemodedefinition;
    props->sleepcurvemodedefinition = sleepcurvemodedefinition;

    size_t buf_len;
    buf_len = iot__service__ac_assistant__properties__get_packed_size(props);
    if (buf_len > 0) {
        buf = malloc(buf_len);
        if (NULL == buf) {
            ret = ENOMEM;
            goto out;
        }
        iot__service__ac_assistant__properties__pack(props, buf);
    }

    struct hey_properties_content content = {
        .n_iid = count,
        .iids = enumerate,
        .len = buf_len,
        .value = buf
    };
    ret = hey_properties_update(ac_assistant_service, &content);

out:
    free(props);
    free(buf);
    if (sleepcurvemodedefinition) {
        for (int i = 0; i < properties->n_sleepcurvemodedefinition; i++) {
            free(sleepcurvemodedefinition[i]);
        }
        free(sleepcurvemodedefinition);
    }
    return ret;
}

/**
 * @brief ac assistant service initialization
 * @param properties [ac assistant service properties]
 * @return int32_t   [initialize result]
*/
int32_t ac_assistant_service_init(const struct ac_assistant_properties *properties)
{
    int ret = 0;
    Iot__Service__AcAssistant__Properties *props = NULL;
    ArrayNode **sleepcurvemodedefinition = NULL;
    props = malloc(sizeof(Iot__Service__AcAssistant__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__ac_assistant__properties__init(props);
    if (NULL != properties) {
        if (properties->n_sleepcurvemodedefinition) {
            sleepcurvemodedefinition = calloc(properties->n_sleepcurvemodedefinition, sizeof(ArrayNode *));
            if (NULL == sleepcurvemodedefinition) {
                ret = ENOMEM;
                goto out;
            }
            for (int i = 0; i < properties->n_sleepcurvemodedefinition; i++) {
                sleepcurvemodedefinition[i] = malloc(sizeof(ArrayNode));
                if (NULL == sleepcurvemodedefinition[i]) {
                    ret = ENOMEM;
                    goto out;
                }
                array_node__init(sleepcurvemodedefinition[i]);
                sleepcurvemodedefinition[i]->id = properties->sleepcurvemodedefinition[i]->id;
                sleepcurvemodedefinition[i]->data.data = properties->sleepcurvemodedefinition[i]->data.data;
                sleepcurvemodedefinition[i]->data.len = properties->sleepcurvemodedefinition[i]->data.len;
            }
        }
        props->power = properties->power;
        props->acmode = properties->acmode;
        props->eco = properties->eco;
        props->sleep = properties->sleep;
        props->rapidmode = properties->rapidmode;
        props->electricheating = properties->electricheating;
        props->targettemperature = properties->targettemperature;
        props->displayingtempunit = properties->displayingtempunit;
        props->fanmode = properties->fanmode;
        props->windspeed = properties->windspeed;
        props->horizontaldirection = properties->horizontaldirection;
        props->verticaldirection = properties->verticaldirection;
        props->sleepcurveenabled = properties->sleepcurveenabled;
        props->sleepcurvemode = properties->sleepcurvemode;
        props->n_sleepcurvemodedefinition = properties->n_sleepcurvemodedefinition;
        props->sleepcurvemodedefinition = sleepcurvemodedefinition;
    }

    ret = ac_assistant_service_register(props);

out:
    free(props);
    if (sleepcurvemodedefinition) {
        for (int i = 0; i < properties->n_sleepcurvemodedefinition; i++) {
            free(sleepcurvemodedefinition[i]);
        }
        free(sleepcurvemodedefinition);
    }
    return ret;
}
