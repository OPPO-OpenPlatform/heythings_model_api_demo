/* Generated by the heythings model compiler.  DO NOT EDIT! */
/* Generated from: smokeSensor.proto */

#ifndef SMOKE_SENSOR_SERVICE_H
#define SMOKE_SENSOR_SERVICE_H

#include "hey/types.h"

struct smoke_sensor_event_smoke_detected {
    char __place_holder__; // make compiler happy
};

struct smoke_sensor_event_no_smoke {
    char __place_holder__; // make compiler happy
};

enum SMOKE_SENSOR_PROPERTY {
    SMOKE_SENSOR_PROPERTY_SMOKECONCENTRATION = 1,
    SMOKE_SENSOR_PROPERTY_ALARMSTATE = 2,
    SMOKE_SENSOR_PROPERTY_SMOKETHRESHOLD = 3
};

enum SMOKE_SENSOR_EVENT {
    SMOKE_SENSOR_EVENT_SMOKE_DETECTED = 4,
    SMOKE_SENSOR_EVENT_NO_SMOKE = 5
};

struct smoke_sensor_properties {
    uint32_t smokeconcentration;
    bool alarmstate;
    uint32_t smokethreshold;
};

/**
 * @brief smoke sensor service initialization
 * @param properties [smoke sensor service properties]
 * @return int32_t   [initialize result]
*/
int32_t smoke_sensor_service_init(const struct smoke_sensor_properties *properties);

/**
 * @brief data at "struct hey_array_node" pack and unpack
*/
/**
 * @brief properties write callback of smoke sensor service
 * @param count      [the number of properties will be written]
 * @param enumerate  [the iid enumeration of properties will be written]
 * @param properties [properties structure with written content]
 * @return int32_t   [be writed result, 0 means success]
*/
int32_t smoke_sensor_properties_write_cb(size_t count, uint32_t *enumerate, struct smoke_sensor_properties *properties);

/**
 * @brief update properties of smoke sensor service
 * @param count      [the number of properties to be updated]
 * @param enumerate  [the iid enumeration of properties to be updated]
 * @param properties [properties structure with updated content]
 * @return int32_t   [update result]
 */
int32_t smoke_sensor_properties_update(size_t count, uint32_t *enumerate, struct smoke_sensor_properties *properties);

/**
 * @brief smoke detected notify of smoke sensor service
 * @param event    [event content]
 * @param attr     [event details]
 * @return int64_t [event uuid, get error code from errno when return negative value]
*/
int64_t smoke_sensor_smoke_detected_notify(struct smoke_sensor_event_smoke_detected *event, struct hey_event_notify_attr *attr);

/**
 * @brief smoke detected notify result callback of smoke sensor service
 * @param ctx    [registered callback context by user]
 * @param result [the result of notify, 0 means success]
 * @return none
*/
void smoke_sensor_smoke_detected_notify_result_cb(struct hey_job_ctx *ctx, int32_t result);

/**
 * @brief no smoke notify of smoke sensor service
 * @param event    [event content]
 * @param attr     [event details]
 * @return int64_t [event uuid, get error code from errno when return negative value]
*/
int64_t smoke_sensor_no_smoke_notify(struct smoke_sensor_event_no_smoke *event, struct hey_event_notify_attr *attr);

/**
 * @brief no smoke notify result callback of smoke sensor service
 * @param ctx    [registered callback context by user]
 * @param result [the result of notify, 0 means success]
 * @return none
*/
void smoke_sensor_no_smoke_notify_result_cb(struct hey_job_ctx *ctx, int32_t result);

#endif /* SMOKE_SENSOR_SERVICE_H */
