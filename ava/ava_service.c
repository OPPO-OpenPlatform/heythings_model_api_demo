/* Generated by the heythings model compiler.  DO NOT EDIT! */
/* Generated from: ava.proto */

#include <errno.h>
#include <assert.h>
#include "ava.pb-c.h"
#include "ava_service.h"
#include "hey/schema.h"

#define AVA_SERVICE 
#define PROP_IID_CNT 2
#define ACT_IID_CNT 2

static const struct hey_schema_property ava_properties_iids[] = {
    [0] = { .iid = 1, .type = HEY_PROPERTY_TYPE_GENERIC },
    [1] = { .iid = 2, .type = HEY_PROPERTY_TYPE_GENERIC },
};
static struct hey_service *ava_service;

static int32_t action_start_ava_start_cb(void *cb_data, struct hey_action_ctx *ctx, size_t len, uint8_t *value, void **user_context)
{
    int32_t ret = -1;
    Iot__Service__Ava__ActionInStartAva *start_ava = iot__service__ava__action_in_start_ava__unpack(NULL, len, value);
    if (NULL == start_ava) {
        hey_action_stop(ctx);
        return EINVAL;
    }

    struct ava_start_ava_in action = { 0 };
    ret = ava_start_ava_start_cb(ctx, &action, user_context);

    iot__service__ava__action_in_start_ava__free_unpacked(start_ava, NULL);
    return ret;
}

static void action_start_ava_stop_cb(void *cb_data, void *user_context)
{
    ava_start_ava_stop_cb(user_context);
}

/**
 * @brief start ava action send message of ava service
 * @param ctx      [action context]
 * @param action   [start ava action out]
 * @return int32_t [action send result, 0 means success]
 */
int32_t ava_start_ava_send(struct hey_action_ctx *ctx, struct ava_start_ava_out *action)
{
    return hey_action_msg_send(ctx, 0, NULL);
}

/**
 * @brief start ava action stop of ava service
 * @param ctx      [action context]
 * @return int32_t [action stop result, 0 means success]
 */
int32_t ava_start_ava_stop(struct hey_action_ctx *ctx)
{
    return hey_action_stop(ctx);
}

static int32_t action_stop_ava_start_cb(void *cb_data, struct hey_action_ctx *ctx, size_t len, uint8_t *value, void **user_context)
{
    int32_t ret = -1;
    Iot__Service__Ava__ActionInStopAva *stop_ava = iot__service__ava__action_in_stop_ava__unpack(NULL, len, value);
    if (NULL == stop_ava) {
        hey_action_stop(ctx);
        return EINVAL;
    }

    struct ava_stop_ava_in action = { 0 };
    ret = ava_stop_ava_start_cb(ctx, &action, user_context);

    iot__service__ava__action_in_stop_ava__free_unpacked(stop_ava, NULL);
    return ret;
}

static void action_stop_ava_stop_cb(void *cb_data, void *user_context)
{
    ava_stop_ava_stop_cb(user_context);
}

/**
 * @brief stop ava action send message of ava service
 * @param ctx      [action context]
 * @param action   [stop ava action out]
 * @return int32_t [action send result, 0 means success]
 */
int32_t ava_stop_ava_send(struct hey_action_ctx *ctx, struct ava_stop_ava_out *action)
{
    return hey_action_msg_send(ctx, 0, NULL);
}

/**
 * @brief stop ava action stop of ava service
 * @param ctx      [action context]
 * @return int32_t [action stop result, 0 means success]
 */
int32_t ava_stop_ava_stop(struct hey_action_ctx *ctx)
{
    return hey_action_stop(ctx);
}

static int32_t service_ava_properties_write_cb(void *cb_data, size_t n_iid, uint32_t *iids,
                                               size_t len, uint8_t *value)
{
    Iot__Service__Ava__Properties *props = iot__service__ava__properties__unpack(NULL, len, value);
    if (NULL == props) {
        return EINVAL;
    }

    int32_t ret = 0;
    struct ava_properties properties = { 0 };
    properties.power = props->power;
    properties.alarmstate = props->alarmstate;
    ret = ava_properties_write_cb(n_iid, iids, &properties);

    iot__service__ava__properties__free_unpacked(props, NULL);
    return ret;
}

static const struct hey_properties_callbacks props_cbs = {
    .write = service_ava_properties_write_cb
};

static const struct hey_action_ctx_callbacks start_ava_cbs = {
    .start = action_start_ava_start_cb,
    .stop = action_start_ava_stop_cb
};

static const struct hey_action_ctx_callbacks stop_ava_cbs = {
    .start = action_stop_ava_start_cb,
    .stop = action_stop_ava_stop_cb
};

static const struct hey_action_entry actions_iids[] = {
    [0] = { .iid = AVA_ACTION_START_AVA, .type = HEY_ACTION_ONCE, .cbs = &start_ava_cbs },
    [1] = { .iid = AVA_ACTION_STOP_AVA, .type = HEY_ACTION_ONCE, .cbs = &stop_ava_cbs },
};

static int ava_service_register(Iot__Service__Ava__Properties *props)
{
    uint8_t *buf;
    size_t buf_len;
    buf_len = iot__service__ava__properties__get_packed_size(props);
    if (buf_len == 0) {
        buf = NULL;
    } else {
        buf = malloc(buf_len);
        if (!buf) {
            return ENOMEM;
        }
        iot__service__ava__properties__pack(props, buf);
    }

    struct hey_schema_service schema_srv;
    hey_schema_service_init(&schema_srv);
    schema_srv.properties.iids = (struct hey_schema_property *)ava_properties_iids;
    schema_srv.properties.n_iid = PROP_IID_CNT;
    schema_srv.properties.value = buf;
    schema_srv.properties.len = buf_len;
    schema_srv.properties.cbs = &props_cbs;
    schema_srv.actions.iids = (struct hey_action_entry *)actions_iids;
    schema_srv.actions.n_iid = ACT_IID_CNT;
    ava_service = hey_service_register(AVA_SERVICE, &schema_srv, NULL);
    if (NULL == ava_service) {
        free(buf);
        return errno;
    }

    free(buf);
    return 0;
}

/**
 * @brief update properties of ava service
 * @param count      [the number of properties to be updated]
 * @param enumerate  [the iid enumeration of properties to be updated]
 * @param properties [properties structure with updated content]
 * @return int32_t   [update result]
 */
int32_t ava_properties_update(size_t count, uint32_t *enumerate, struct ava_properties *properties)
{
    assert(count <= PROP_IID_CNT);
    if (NULL == properties) {
        return EINVAL;
    }

    int ret = 0;
    Iot__Service__Ava__Properties *props = NULL;
    uint8_t *buf = NULL;
    props = malloc(sizeof(Iot__Service__Ava__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__ava__properties__init(props);
    props->power = properties->power;
    props->alarmstate = properties->alarmstate;

    size_t buf_len;
    buf_len = iot__service__ava__properties__get_packed_size(props);
    if (buf_len > 0) {
        buf = malloc(buf_len);
        if (NULL == buf) {
            ret = ENOMEM;
            goto out;
        }
        iot__service__ava__properties__pack(props, buf);
    }

    struct hey_properties_content content = {
        .n_iid = count,
        .iids = enumerate,
        .len = buf_len,
        .value = buf
    };
    ret = hey_properties_update(ava_service, &content);

out:
    free(props);
    free(buf);
    return ret;
}

/**
 * @brief ava service initialization
 * @param properties [ava service properties]
 * @return int32_t   [initialize result]
*/
int32_t ava_service_init(const struct ava_properties *properties)
{
    int ret = 0;
    Iot__Service__Ava__Properties *props = NULL;
    props = malloc(sizeof(Iot__Service__Ava__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__ava__properties__init(props);
    if (NULL != properties) {
        props->power = properties->power;
        props->alarmstate = properties->alarmstate;
    }

    ret = ava_service_register(props);

out:
    free(props);
    return ret;
}
