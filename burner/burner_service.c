/* Generated by the heythings model compiler.  DO NOT EDIT! */
/* Generated from: burner.proto */

#include <errno.h>
#include <assert.h>
#include "burner.pb-c.h"
#include "burner_service.h"
#include "hey/schema.h"

#define BURNER_SERVICE (83 * 256)
#define PROP_IID_CNT 15
#define ACT_IID_CNT 1
#define EV_IID_CNT 3

static const struct hey_schema_property burner_properties_iids[] = {
    [0] = { .iid = 2, .type = HEY_PROPERTY_TYPE_GENERIC },
    [1] = { .iid = 3, .type = HEY_PROPERTY_TYPE_GENERIC },
    [2] = { .iid = 4, .type = HEY_PROPERTY_TYPE_GENERIC },
    [3] = { .iid = 5, .type = HEY_PROPERTY_TYPE_GENERIC },
    [4] = { .iid = 6, .type = HEY_PROPERTY_TYPE_GENERIC },
    [5] = { .iid = 7, .type = HEY_PROPERTY_TYPE_GENERIC },
    [6] = { .iid = 8, .type = HEY_PROPERTY_TYPE_GENERIC },
    [7] = { .iid = 9, .type = HEY_PROPERTY_TYPE_GENERIC },
    [8] = { .iid = 10, .type = HEY_PROPERTY_TYPE_GENERIC },
    [9] = { .iid = 11, .type = HEY_PROPERTY_TYPE_GENERIC },
    [10] = { .iid = 15, .type = HEY_PROPERTY_TYPE_GENERIC },
    [11] = { .iid = 16, .type = HEY_PROPERTY_TYPE_GENERIC },
    [12] = { .iid = 17, .type = HEY_PROPERTY_TYPE_GENERIC },
    [13] = { .iid = 18, .type = HEY_PROPERTY_TYPE_GENERIC },
    [14] = { .iid = 19, .type = HEY_PROPERTY_TYPE_GENERIC },
};
static const uint32_t burner_event_iids[] = { 12, 13, 14 };
static struct hey_service *burner_service;

static int32_t action_turn_off_fire_start_cb(void *cb_data, struct hey_action_ctx *ctx, size_t len, uint8_t *value, void **user_context)
{
    int32_t ret = -1;
    Iot__Service__Burner__ActionInTurnOffFire *turn_off_fire = iot__service__burner__action_in_turn_off_fire__unpack(NULL, len, value);
    if (NULL == turn_off_fire) {
        hey_action_stop(ctx);
        return EINVAL;
    }

    struct burner_turn_off_fire_in action = { 0 };
    ret = burner_turn_off_fire_start_cb(ctx, &action, user_context);

    iot__service__burner__action_in_turn_off_fire__free_unpacked(turn_off_fire, NULL);
    return ret;
}

static void action_turn_off_fire_stop_cb(void *cb_data, void *user_context)
{
    burner_turn_off_fire_stop_cb(user_context);
}

/**
 * @brief turn off fire action send message of burner service
 * @param ctx      [action context]
 * @param action   [turn off fire action out]
 * @return int32_t [action send result, 0 means success]
 */
int32_t burner_turn_off_fire_send(struct hey_action_ctx *ctx, struct burner_turn_off_fire_out *action)
{
    return hey_action_msg_send(ctx, 0, NULL);
}

/**
 * @brief turn off fire action stop of burner service
 * @param ctx      [action context]
 * @return int32_t [action stop result, 0 means success]
 */
int32_t burner_turn_off_fire_stop(struct hey_action_ctx *ctx)
{
    return hey_action_stop(ctx);
}

static int32_t service_burner_properties_write_cb(void *cb_data, size_t n_iid, uint32_t *iids,
                                                  size_t len, uint8_t *value)
{
    Iot__Service__Burner__Properties *props = iot__service__burner__properties__unpack(NULL, len, value);
    if (NULL == props) {
        return EINVAL;
    }

    int32_t ret = 0;
    struct burner_properties properties = { 0 };
    properties.heatinglevel = props->heatinglevel;
    properties.targettemperature = props->targettemperature;
    properties.menumode = props->menumode;
    properties.weight = props->weight;
    properties.thickness = props->thickness;
    properties.steaklevel = props->steaklevel;
    properties.concentration = props->concentration;
    properties.operationtime = props->operationtime;
    properties.currenttemperature = props->currenttemperature;
    properties.machinestatus = props->machinestatus;
    properties.constanttempenabled = props->constanttempenabled;
    properties.appointmentpoweroff = props->appointmentpoweroff;
    properties.firestatus = props->firestatus;
    properties.runningduration = props->runningduration;
    properties.remaintime = props->remaintime;
    ret = burner_properties_write_cb(n_iid, iids, &properties);

    iot__service__burner__properties__free_unpacked(props, NULL);
    return ret;
}

static const struct hey_properties_callbacks props_cbs = {
    .write = service_burner_properties_write_cb
};

static const struct hey_action_ctx_callbacks turn_off_fire_cbs = {
    .start = action_turn_off_fire_start_cb,
    .stop = action_turn_off_fire_stop_cb
};

static const struct hey_action_entry actions_iids[] = {
    [0] = { .iid = BURNER_ACTION_TURN_OFF_FIRE, .type = HEY_ACTION_ONCE, .cbs = &turn_off_fire_cbs },
};

static int burner_service_register(Iot__Service__Burner__Properties *props)
{
    uint8_t *buf;
    size_t buf_len;
    buf_len = iot__service__burner__properties__get_packed_size(props);
    if (buf_len == 0) {
        buf = NULL;
    } else {
        buf = malloc(buf_len);
        if (!buf) {
            return ENOMEM;
        }
        iot__service__burner__properties__pack(props, buf);
    }

    struct hey_schema_service schema_srv;
    hey_schema_service_init(&schema_srv);
    schema_srv.properties.iids = (struct hey_schema_property *)burner_properties_iids;
    schema_srv.properties.n_iid = PROP_IID_CNT;
    schema_srv.properties.value = buf;
    schema_srv.properties.len = buf_len;
    schema_srv.properties.cbs = &props_cbs;
    schema_srv.actions.iids = (struct hey_action_entry *)actions_iids;
    schema_srv.actions.n_iid = ACT_IID_CNT;
    schema_srv.events.iids = (uint32_t *)burner_event_iids;
    schema_srv.events.n_iid = EV_IID_CNT;
    burner_service = hey_service_register(BURNER_SERVICE, &schema_srv, NULL);
    if (NULL == burner_service) {
        free(buf);
        return errno;
    }

    free(buf);
    return 0;
}

/**
 * @brief update properties of burner service
 * @param count      [the number of properties to be updated]
 * @param enumerate  [the iid enumeration of properties to be updated]
 * @param properties [properties structure with updated content]
 * @return int32_t   [update result]
 */
int32_t burner_properties_update(size_t count, uint32_t *enumerate, struct burner_properties *properties)
{
    assert(count <= PROP_IID_CNT);
    if (NULL == properties) {
        return EINVAL;
    }

    int ret = 0;
    Iot__Service__Burner__Properties *props = NULL;
    uint8_t *buf = NULL;
    props = malloc(sizeof(Iot__Service__Burner__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__burner__properties__init(props);
    props->heatinglevel = properties->heatinglevel;
    props->targettemperature = properties->targettemperature;
    props->menumode = properties->menumode;
    props->weight = properties->weight;
    props->thickness = properties->thickness;
    props->steaklevel = properties->steaklevel;
    props->concentration = properties->concentration;
    props->operationtime = properties->operationtime;
    props->currenttemperature = properties->currenttemperature;
    props->machinestatus = properties->machinestatus;
    props->constanttempenabled = properties->constanttempenabled;
    props->appointmentpoweroff = properties->appointmentpoweroff;
    props->firestatus = properties->firestatus;
    props->runningduration = properties->runningduration;
    props->remaintime = properties->remaintime;

    size_t buf_len;
    buf_len = iot__service__burner__properties__get_packed_size(props);
    if (buf_len > 0) {
        buf = malloc(buf_len);
        if (NULL == buf) {
            ret = ENOMEM;
            goto out;
        }
        iot__service__burner__properties__pack(props, buf);
    }

    struct hey_properties_content content = {
        .n_iid = count,
        .iids = enumerate,
        .len = buf_len,
        .value = buf
    };
    ret = hey_properties_update(burner_service, &content);

out:
    free(props);
    free(buf);
    return ret;
}

/**
 * @brief burner service initialization
 * @param properties [burner service properties]
 * @return int32_t   [initialize result]
*/
int32_t burner_service_init(const struct burner_properties *properties)
{
    int ret = 0;
    Iot__Service__Burner__Properties *props = NULL;
    props = malloc(sizeof(Iot__Service__Burner__Properties));
    if (NULL == props) {
        ret = ENOMEM;
        goto out;
    }
    iot__service__burner__properties__init(props);
    if (NULL != properties) {
        props->heatinglevel = properties->heatinglevel;
        props->targettemperature = properties->targettemperature;
        props->menumode = properties->menumode;
        props->weight = properties->weight;
        props->thickness = properties->thickness;
        props->steaklevel = properties->steaklevel;
        props->concentration = properties->concentration;
        props->operationtime = properties->operationtime;
        props->currenttemperature = properties->currenttemperature;
        props->machinestatus = properties->machinestatus;
        props->constanttempenabled = properties->constanttempenabled;
        props->appointmentpoweroff = properties->appointmentpoweroff;
        props->firestatus = properties->firestatus;
        props->runningduration = properties->runningduration;
        props->remaintime = properties->remaintime;
    }

    ret = burner_service_register(props);

out:
    free(props);
    return ret;
}

/**
 * @brief dry braised notify of burner service
 * @param event    [event content]
 * @param attr     [event details]
 * @return int64_t [event uuid, get error code from errno when return negative value]
*/
int64_t burner_dry_braised_notify(struct burner_event_dry_braised *event, struct hey_event_notify_attr *attr)
{
    int64_t ret = -1;

    struct hey_event_notify_content content = {
        .iid = BURNER_EVENT_DRY_BRAISED,
        .importance = attr->importance,
        .uuid = attr->uuid,
        .ref_uuid = attr->ref_uuid,
        .timestamp = attr->timestamp,
        .len = 0,
        .value = NULL,
    };
    ret = hey_event_notify(burner_service, &content, burner_dry_braised_notify_result_cb, attr->ctx);

    return ret;
}

/**
 * @brief gas detected notify of burner service
 * @param event    [event content]
 * @param attr     [event details]
 * @return int64_t [event uuid, get error code from errno when return negative value]
*/
int64_t burner_gas_detected_notify(struct burner_event_gas_detected *event, struct hey_event_notify_attr *attr)
{
    int64_t ret = -1;

    struct hey_event_notify_content content = {
        .iid = BURNER_EVENT_GAS_DETECTED,
        .importance = attr->importance,
        .uuid = attr->uuid,
        .ref_uuid = attr->ref_uuid,
        .timestamp = attr->timestamp,
        .len = 0,
        .value = NULL,
    };
    ret = hey_event_notify(burner_service, &content, burner_gas_detected_notify_result_cb, attr->ctx);

    return ret;
}

/**
 * @brief burner event information notify of burner service
 * @param event    [event content]
 * @param attr     [event details]
 * @return int64_t [event uuid, get error code from errno when return negative value]
*/
int64_t burner_burner_event_information_notify(struct burner_event_burner_event_information *event, struct hey_event_notify_attr *attr)
{
    int64_t ret = -1;

    Iot__Service__Burner__EventBurnerEventInformation *ev = NULL;
    uint8_t *buf = NULL;
    ev = malloc(sizeof(Iot__Service__Burner__EventBurnerEventInformation));
    if (NULL == ev) {
        errno = ENOMEM;
        return ret;
    }
    iot__service__burner__event_burner_event_information__init(ev);
    ev->messageno = event->messageno;

    size_t buf_len;
    buf_len = iot__service__burner__event_burner_event_information__get_packed_size(ev);
    if (buf_len > 0) {
        buf = malloc(buf_len);
        if (NULL == buf) {
            errno = ENOMEM;
            goto out;
        }
        iot__service__burner__event_burner_event_information__pack(ev, buf);
    }

    struct hey_event_notify_content content = {
        .iid = BURNER_EVENT_BURNER_EVENT_INFORMATION,
        .importance = attr->importance,
        .uuid = attr->uuid,
        .ref_uuid = attr->ref_uuid,
        .timestamp = attr->timestamp,
        .len = buf_len,
        .value = buf,
    };
    ret = hey_event_notify(burner_service, &content, burner_burner_event_information_notify_result_cb, attr->ctx);

out:
    free(ev);
    free(buf);
    return ret;
}
